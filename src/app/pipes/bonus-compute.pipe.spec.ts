import { BonusComputePipe } from './bonus-compute.pipe';

describe('BonusComputePipe', () => {
  it('create an instance', () => {
    const pipe = new BonusComputePipe();
    expect(pipe).toBeTruthy();
  });
});
