import { ContractTypeMapPipe } from './contract-type-map.pipe';

describe('ContractTypeMapPipe', () => {
  it('create an instance', () => {
    const pipe = new ContractTypeMapPipe();
    expect(pipe).toBeTruthy();
  });
});
